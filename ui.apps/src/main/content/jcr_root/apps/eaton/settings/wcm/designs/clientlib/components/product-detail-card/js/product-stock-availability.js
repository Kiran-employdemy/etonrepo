/**
 *
 *
 *
 * - THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY -
 * - Generated by Gulp (gulp-babel).
 *
 *
 *
 *
 */


'use strict';

//-----------------------------------
// free-Sample-Stock-Availability
// -----------------------------------

var addNewbutton = document.getElementById('addNewbutton');
var mousCountry = '';
var productAbl = '';
var newcart = document.getElementById('newcart');
var _endIndexSkuPage = $(location).attr('pathname').indexOf('skuPage.') + 8;
var catalogNumber = $(location).attr('pathname').substring(_endIndexSkuPage, $(location).attr('pathname').indexOf('.', _endIndexSkuPage));

// catalog API

function catalogAPI() {
  $.ajax({
    type: 'GET',
    url: '/eaton/services/freeSampleStockAvailability.catalogId$' + catalogNumber + '.html',
    async: false,
    success: function success(response) {
      if (response) {
        if (typeof response === 'string') {
          productAbl = JSON.parse(response);
        } else {
          productAbl = response;
        }
      }

      checkCountry(productAbl);
    },
    error: function error(response) {
      console.log('ERROR', response);
    }
  });
}

if (addNewbutton) {

  catalogAPI();
  findCountry();

  // For user cart checking

  var linkattr = document.querySelector('a.free-btn');
  var hasattr = linkattr.hasAttribute('data-cta-link');

  if (hasattr) {
    var attrValue = linkattr.getAttribute('data-cta-link');
    linkattr.setAttribute('href', attrValue);
  } else {
    linkattr.setAttribute('id', 'checkUser');
    linkattr.setAttribute('href', 'javascript:void(0)');
  }

  var checkUser = document.getElementById('checkUser');

  if (checkUser) {
    document.getElementById('checkUser').addEventListener('click', function () {
      checkUserCart();
    });
  }
  // Create a new Cart for product

  newcart.onclick = function () {
    deleteCart();
  };
}

// Check  mouser Country for product

function checkCountry(productAbl) {

  if (productAbl && productAbl !== {} && !$.isEmptyObject(productAbl)) {

    var vals = productAbl.product.Availability;

    if (vals === 'yes') {

      if (mousCountry === 'us' || mousCountry === 'ca' || mousCountry === 'mx' || mousCountry === 'jp' || mousCountry === 'cn') {

        $('.sample-btn a').removeClass('hide');
      } else {

        $('.sample-btn a').removeClass('hide');
      }
    } else {

      return false;
    }
  } else {

    return false;
  }
}

// Find country from URL

function findCountry() {
  var pathVal = location.pathname;
  var tempVal = '';
  tempVal = pathVal.split('.');
  tempVal = tempVal[0].split('-');
  mousCountry = tempVal[1].split('/')[0];
}

// Check user has cart or not

function checkUserCart() {
  $.ajax({
    type: 'GET',
    url: '/eaton/secure/ecommerce/cart',
    success: function success(response) {
      if (response) {
        var overview = '';
        if (typeof response === 'string') {
          overview = JSON.parse(response);
        } else {
          overview = response;
        }
        checkProductPrice(overview);
      }
    },
    error: function error(response) {
      console.log('ERROR', response);
    }
  });
}

// Check product price and same catalog number

function checkProductPrice(response) {
  if (!response.cartItems) {
    return false;
  }

  var unitprice = response.cartItems.map(function (e) {
    return e.unitPrice;
  });

  var catalogNumCart = response.cartItems.map(function (e) {
    return e.catalogNumber;
  });
  var cartitemCount = response.cartItems.length;

  if (cartitemCount === 0) {
    sendCart();
    redirectPage();
  } else {

    if (unitprice[0] === 0) {
      if (catalogNumCart.indexOf(catalogNumber) !== -1) {
        $('#same-prod').modal('show');
      } else {
        sendCart();
        redirectPage();
      }
    } else {
      $('#prod-sold').modal('show');
    }
  }
}

function redirectPage() {

  var path = window.location.href.split('/').slice(0, -1).join('/');
  window.location = path + '/secure/shopping-cart.html';
  // update cart count value after adding a cart
  var getCookieValue = $.cookie('etn-cart-count');
  if (getCookieValue) {
    var newCount = parseInt(getCookieValue) + 1;
    $.cookie('etn-cart-count', newCount, { path: '/', domain: window.location.hostname });
  }
}

function sendCart() {

  var customerNumber = '000123';
  var erpsystem = 'MFGSOR';
  var productpath = location.pathname;

  var value2 = {
    customerNumber: customerNumber,
    cartType: 'Sample',
    salesOrg: {
      org: '',
      division: '',
      channel: ''
    },
    cartItems: [{
      productId: catalogNumber,
      productIdQualifier: 'CAT',
      unitPrice: 0,
      quantity: 1,
      productPath: productpath
    }]
  };

  $.ajax({
    type: 'POST',
    url: '/eaton/secure/ecommerce/cart?operation=addtocart&erpSystem=' + erpsystem,
    data: JSON.stringify(value2),
    dataType: 'json',
    headers: { 'Content-Type': 'application/json' },
    success: function success(response) {
      console.log(response);
    }

  });
}

function deleteCart() {
  $.ajax({
    type: 'DELETE',
    url: '/eaton/secure/ecommerce/cart?operation=deleteentirecart',
    success: function success(response) {
      console.log(response);
    }

  });
  $.removeCookie('etn-cart-count');
  sendCart();
  redirectPage();
}